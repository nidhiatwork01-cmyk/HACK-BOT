Flask==3.0.0
flask-cors==4.0.0
PyJWT==2.8.0
bcrypt==4.1.2
python-dotenv==1.0.0
# scikit-learn and sentence-transformers are optional for ML features
# The code has fallbacks that work without them
# If you want full ML features, install them separately later

